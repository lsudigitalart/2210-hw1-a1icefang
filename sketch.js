function setup () {
    createCanvas (800,600)
    ellipse(width / 2, height, 2, 200, 10);
    ellipse(width / 2, height / 2, 100, 100);
    ellipse(width / 2, height / 2, 10, 100);
}

function draw() {
    
}